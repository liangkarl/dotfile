#!/bin/sh

usage() {
	cat << USAGE
Replace windows line break to POSIX style

Usage: $(basename $0) [file]

Example:
  $ fromdos dos.txt
USAGE
	return 1
}


if [[ ! -f "$1" ]]; then
	usage
else
	sed -i 's/\r$//' $1
fi

exit $?
