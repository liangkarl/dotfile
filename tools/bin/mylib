#!/usr/bin/env bash

if [[ -z "$SHELL_DIR" ]]; then
	echo "shell-dir-not-defined"
	exit 1
fi

if [[ ! -e "$SHELL_DIR" ]]; then
	echo "shell-dir-not-existed"
	exit 2
fi

if [[ ! -e "$SHELL_DIR/lib/$1" ]]; then
	echo "script-not-existed"
	exit 3
fi

echo "$SHELL_DIR/lib/$1"
