# required
remote='karl@127.0.0.1'
remote_root=(
"$(pwd)"
"$(dirname $(readlink -e $0))"
)

# optional
default_local_dir="images@$(date +%Y%m%d-%H%M%S)"
default_remote_files=(
"aaa.img"
"bbb.bin"
"ccc.xz"
)
