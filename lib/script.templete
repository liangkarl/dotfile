#!/bin/bash

source $SHELL_CORE_DIR/utils.sh
# source $SHELL_CORE_DIR/sign.sh

XXXX='xxxx'
XXXX_DIR="$SHELL_CONFIG_DIR/$XXXX"

install()
{
	local NEED_PACK='

	'
	has_these_cmds "$NEED_PACK" || {
		show_err "Incompleted installization of $NEED_PACK"
		return
	}
	echo "Install $XXXX..."
	echo "Not finished yet..."
	# config_package
}

uninstall()
{
	echo "Remove $XXXX..."
	echo "Not finished yet..."
}

config_package()
{
	echo "Config $XXXX..."
	echo "Not finished yet..."
}
