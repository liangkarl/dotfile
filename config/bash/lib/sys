has_cmd() {
    command -v "$1"
    return $?
} &> /dev/null

has_cmds() {
    local cmd num

    num=0
    for cmd in $@; do
        has_cmd $cmd || (( num++ ))
    done

    return $num
} &> /dev/null

in_wsl() {
	if [[ -z "$WSL_DISTRO" ]]; then
		return 1
	else
		return 0
	fi
}
